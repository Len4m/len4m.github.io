---
import Layout from "@layouts/Layout.astro";
import Header from "@components/Header.astro";
import Footer from "@components/Footer.astro";
import McpCreatorComponent from "@components/McpCreator";
---

<Layout
  title="MCP Server Generator - Create Model Context Protocol Servers from Binary Help | Lenam"
  description="Generate complete MCP (Model Context Protocol) servers automatically from binary help text. Supports Node.js and Python templates. Parse command-line options, flags, and arguments to create production-ready MCP servers for any CLI tool."
>
  <Header />

  <main id="main-content">
    <article id="article" class="prose mx-auto mt-8 max-w-3xl px-4">
      <div class="mb-8">
        <h1 class="mb-4 text-3xl font-bold text-skin-accent">
          MCP Server Generator
        </h1>
        <blockquote class="mb-6 border-l-4 border-skin-accent pl-4">
          <p class="mb-2 text-sm text-skin-base">
            MCP (Model Context Protocol) is a protocol that enables LLMs to
            connect with external tools, data sources, and systems.
          </p>
          <a
            href="https://modelcontextprotocol.io/docs/mcp"
            target="_blank"
            rel="noopener noreferrer"
            class="text-sm font-medium text-skin-accent hover:underline"
          >
            Official MCP documentation →
          </a>
        </blockquote>
        <p>
          <strong>⚠️ Beta Version:</strong> This tool is under active development.
          Use it with caution and always review the generated code before running it
          in production.
          <br /><br />
          Automatically generate MCP (Model Context Protocol) servers from binary
          help documentation. The help analyzer is a useful starting point, but
          you can also manually configure all parameters according to your
          specific needs.
        </p>
      </div>

      <McpCreatorComponent client:load />
    </article>
  </main>

  <Footer />
</Layout>

